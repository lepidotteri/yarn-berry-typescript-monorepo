{
  "name": "@yarn-berry-typescript-monorepo/scripts",
  "private": true,
  "scripts": {
    "clean": "echo \"Nothing to clean\" && exit 0",
    "prebuild": "yarn clean",
    "format": "deno fmt",
    "lint": "deno lint --unstable",
    "test": "echo \"No tests\" && exit 0"
  }
}
